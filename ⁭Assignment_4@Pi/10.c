/* Write a program to print a table of N */


#include<stdio.h>
int main()
{
    double i;
    printf("Table of N \n");
    
    
    for(i=1; i<=i+1; i++)
    {
        printf("%.1f * %.1f = %.1f\n", i+1, i+1, i*i+1);
        
    }

    //while (i<i+1)
    {
        
        //i++;
       // printf("%.1f * %.1f = %.1f\n", i+1, i+1, i*i+1);
        
        
   }
    
    return 0;
}